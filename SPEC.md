# SPEC.md — Pixel Siege: Elite リメイク仕様書

> バージョン: v1.0.0（2026-08-23 実装完了）
> ステータス: 実装済み（実装時の判断で仕様を調整した箇所は「14. 実装メモ」を参照）

---

## 1. ゲーム概要

**Pixel Siege: Elite** は、スマートフォン縦画面向けのカジュアルオートバトラーゲームです。

### コンセプト
- Mechabellum を参考にしたカジュアル寄りのオートバトラー
- 準備フェーズ（ユニット購入・配置）→バトルフェーズ（全自動）のサイクル
- シンプルな操作でプレイできるが戦略的な編成が重要

### ターゲット
- デバイス: スマートフォン縦画面（iOS / Android）
- PC でもプレイ可能（縦長レイアウト固定で同じ見た目）

---

## 2. 画面レイアウト仕様

### 縦画面固定

- **ゲームエリアサイズ**: 最大幅 `390px`、縦は画面全体を使用
- **PC 表示**: ゲームエリアを中央に固定、左右は黒（レターボックス）
- スマホ表示と PC 表示で同一の見た目になるように統一

```css
/* 概念イメージ */
.game-wrapper {
    max-width: 390px;
    margin: 0 auto;
}
```

### フィールド方向

- 縦方向固定（変更なし）
- **上部**: 敵の陣地・敵ユニットのスポーン位置
- **下部**: プレイヤー陣地・拠点オブジェクト

---

## 3. ゲームモード

### モード 1: ステージ制（モード名: **STORY**）

ウェーブ＋ボスのステージ形式。7 ステージ構成（現行と同じ）。

```
準備フェーズ → バトルフェーズ → 結果 → ショップ（ユニット追加購入） → 次ウェーブ
```

- ウェーブクリアごとに追加予算を獲得
- ボス撃破後に次ウェーブへ

### モード 2: AI 対戦（モード名: **BATTLE**）

プレイヤー vs AI の 1 対 1。同一条件（同じ予算）で戦う。

```
準備フェーズ（両者同時） → バトルフェーズ → 勝敗判定 → 次ラウンド
```

- ラウンドを重ねるごとに予算増加・難易度上昇
- AI は難易度レベルに応じた編成プリセットから選択

---

## 4. 予算システム（マナ廃止）

### 基本ルール

- マナ（自然回復制）は**完全廃止**
- 代わりに**ラウンド開始時に固定ゴールドを配布**
- 予算はユニット購入のみに使用（バトル中の消費なし）

### 予算テーブル（案）

| ウェーブ / ラウンド | 配布予算 |
|----------------|---------|
| 1 | 80G |
| 2 | 110G |
| 3 | 140G |
| 4 | 180G |
| 5 | 220G |
| 6 | 270G |
| 7 | 320G |

### 購入ルール

- 準備フェーズ中のみ購入可能
- 残予算内であれば何体でも購入可
- 同種ユニットの複数購入可

---

## 5. フェーズ仕様

### 準備フェーズ

- **制限時間: なし**（プレイヤーが「バトル開始」ボタンを押すまで）
- ユニットショップを表示
- 購入したユニットをフィールド下部（プレイヤーエリア）にドラッグ or タップで配置
- 残予算・購入済みユニット一覧を常時表示
- AI 対戦モードでは AI も同時に（裏で）編成を決定（可視化なし）

### バトルフェーズ

- 「バトル開始」を押すと即座に開始
- **プレイヤー操作は一切不可**（完全観戦）
- ユニットは自動的に動き、攻撃し、拠点を目指す
- 勝利条件: 敵拠点の HP を 0 にする
- 敗北条件: 自拠点の HP が 0 になる

---

## 6. 拠点オブジェクト仕様

### ビジュアル

- 新規作成の城ドット絵スプライト（16×14 ピクセル）
- プレイヤー拠点: 画面下部中央に配置、緑系カラーパレット
- 敵拠点（STORY ボスモード以外）: 画面上部中央に配置、赤系カラーパレット

### 動作

- 敵ユニットが拠点の近く（攻撃範囲内）に入ると攻撃される
- 拠点自体は移動しない固定オブジェクト
- HP が 0 になるとゲームオーバー
- HP バーを拠点オブジェクトの上に表示

### ステータス

| 項目 | 実装値 |
|-----|---------|
| HP | 1500（強化「城壁補強」で +400 ずつ増加） |
| 攻撃を受ける範囲 | 拠点中心から 50px 前後（当たり判定半径 26px + ユニットの射程） |
| 攻撃を受けたときのエフェクト | フラッシュ + シェイク |
| HP バー | 城スプライトの上に表示 |

---

## 7. ユニットショップ仕様

### 概要

ランダム 3 択の報酬選択を廃止し、**常時全ユニット一覧を表示するショップ**に変更。

### 表示レイアウト

- カード形式でユニット一覧を表示（スクロール可）
- 各カードに以下を表示:
  - ユニットのドット絵アイコン
  - ユニット名
  - コスト（ゴールド）
  - ステータス一覧（下記参照）
  - 1 行の特徴コメント

### 表示ステータス一覧

| 表示名 | 内部パラメータ | 説明 |
|------|-------------|------|
| **HP** | `hp` | 体力 |
| **ATK** | `dmg` | 攻撃力（ダメージ/回） |
| **ATK SPD** | `rate` | 攻撃間隔（低いほど速い） |
| **MOV** | `speed` | 移動速度 |
| **RANGE** | `range` | 攻撃射程 |
| **TYPE** | `type` | 攻撃タイプ |
| **コスト** | `cost` | 購入に必要なゴールド |

> **ノックバック**については数値表示ではなく、カードの特徴コメント欄に「近接攻撃で相手を押し返す」などのテキストで表現する。

### ユニット種類（プレイヤー側）

| ユニット | 表示名 | タイプ | コスト | 特徴コメント |
|---------|------|------|------|------------|
| knight | KNIGHT | 近接 | 20G | 重装甲の前衛。敵を力強く押し返す |
| archer | ARCHER | 遠距離 | 30G | 安全な距離から弓で攻撃する |
| wizard | MAGE | 範囲 | 55G | 広範囲の魔法攻撃。群れに有効 |
| healer | CLERIC | 回復 | 45G | 味方ユニットのHPを回復する |
| giant | GOLEM | タンク | 90G | 超高耐久の盾役。圧倒的な存在感 |

### 配置上限と編成の引き継ぎ

- 同時に配置できるのは **15 体**まで（画面が埋まりすぎないための制限）
- バトル後、**生き残ったユニットは次ラウンドに引き継がれる**（HP は全回復）
- 準備フェーズ中は配置済みユニットを **ドラッグで移動 / タップで売却（全額返金）**
- 上限に達して予算が余った場合は「強化」「戦術」タブで消費する

---

## 8. ビジュアル修正項目

### 修正が必要なバグ・ズレ

1. **ボスの影の位置**: 楕円シャドウが本体と一致していない → 影の描画位置を本体スプライトの底部に合わせる
2. **ボスのスプライト反転**: `ctx.scale(1, -1)` によるフリップ後の位置計算がずれている → 描画原点の再計算
3. **ユニットの HP バーの位置**: スプライトの描画スケールに対して HP バーが合っていない
4. **PC 表示の横広がり**: キャンバスが画面幅いっぱいに広がってしまう → `max-width: 390px` 固定で解消

---

## 9. PWA 対応

### マニフェスト設定

- ファイル: `manifest.json`（新規作成）
- `index.html` に `<link rel="manifest" href="manifest.json">` を追加

### アイコン

- 使用スプライト: `SPRITES.knight`（歩兵ユニット、プレイヤー側の代表キャラ）
- カラーパレット: `PALETTES.knight` を使用
- サイズ: 192×192px、512×512px（ドット絵を拡大）
- 生成方法: Canvas API で描画し PNG として `icons/` フォルダに保存

### manifest.json 設定内容

```json
{
  "name": "Pixel Siege: Elite",
  "short_name": "Pixel Siege",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#0f172a",
  "theme_color": "#0f172a",
  "icons": [
    { "src": "icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "icons/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```

---

## 10. AI 対戦モード仕様（詳細）

### AI 編成ロジック

- 難易度（Easy / Normal / Hard）ごとに編成プリセットを保有
- 予算内でプリセットに従いユニットを選択・配置
- 配置位置は上部エリアにランダムで分散

### 難易度設定（案）

| 難易度 | AI の傾向 | 説明 |
|------|---------|------|
| Easy | 安くて数が多い | goblin、orc 中心 |
| Normal | バランス型 | knight、archer 混成 |
| Hard | 高コスト特化 | wizard、giant 中心 |

---

## 11. 開発優先順位

| 優先度 | 項目 |
|------|------|
| 🔴 高 | 縦画面固定レイアウト |
| 🔴 高 | ビジュアルバグ修正（ボス影・HP バー） |
| 🔴 高 | マナ→予算システム刷新 |
| 🔴 高 | 準備フェーズ追加 |
| 🔴 高 | 拠点オブジェクト化・城スプライト新規作成 |
| 🔴 高 | ユニットショップ実装 |
| 🟡 中 | PWA マニフェスト・アイコン |
| 🟡 中 | STORY モード完成 |
| 🟡 中 | BATTLE（AI 対戦）モード追加 |
| 🟢 低 | AI 難易度調整 |
| 🟢 低 | スキルシステムの見直し |

すべて実装済み。

---

## 12. 強化・戦術（ショップの追加タブ）

準備フェーズのショップは 3 タブ構成。

| タブ | 内容 |
|-----|-----|
| ユニット | 全ユニットを常時表示。予算内で何体でも購入可 |
| 強化 | 恒久効果のアップグレード。購入のたびに価格が 1.6 倍になる |
| 戦術 | バトル中に自動発動する必殺技。1 種類につき 1 回だけ購入できる |

### 強化一覧

攻撃強化(+15%) / 装甲強化(HP+20%) / 速射訓練(攻撃間隔-12%) / 進軍訓練(移動+20%) /
射程延長(+15%) / 城壁補強(拠点HP+400) / 自動修復(毎秒8回復) / 反射装甲(被ダメ25%反射) /
吸血の紋章(与ダメ10%吸収)

### 戦術一覧

| 戦術 | 効果 | CD |
|-----|------|-----|
| メテオストーム | 敵全体に120ダメージ | 20秒 |
| マスヒール | 味方全体のHPを35%回復 | 24秒 |
| タイムワープ | 敵の移動速度を6秒間半減 | 22秒 |
| 守護天使 | 守護天使を20秒間召喚 | 30秒 |

---

## 13. 勝敗条件

| モード | 勝利 | 敗北 |
|-------|-----|-----|
| STORY | ウェーブの敵を全滅させ、ボスを撃破する | 自拠点 HP が 0 / 制限時間(150秒)切れ |
| BATTLE | 敵拠点 HP を 0 にする | 自拠点 HP が 0 / 制限時間(90秒)時点で拠点HP割合が下回る |

敗北しても結果画面から「もう一度挑戦する」で、そのラウンドの準備フェーズ開始時点からやり直せる。

---

## 14. 実装メモ（仕様からの調整点）

1. **スプライトの上下反転を廃止**
   仕様 8-2 は `ctx.scale(1, -1)` のフリップ位置補正だったが、上下反転はキャラクターが
   逆さまに描画されるため取りやめ、敵は**左右反転**で向かい合わせる方式に変更した。
   併せて描画を `drawSprite()` に共通化し、影・HP バーをスプライト実寸から計算するようにした。

2. **ボスパレットの色数不足を修正**
   ボススプライトはパレット index 8〜10 を使用しているのに定義が 6 要素しかなく、
   一部が未定義色で描画されていた。全ボスパレットを 11 要素に拡張した。

3. **アクティブスキルの廃止 → 戦術（自動発動）へ**
   「バトル中はプレイヤー操作不可」という仕様と、手動発動スキルは両立しないため、
   スキルは購入型の「戦術」として再設計し、クールダウンごとに自動発動する形にした。
   マナ関連のアップグレードは予算制への移行に伴い削除した。

4. **バランス調整**
   ヘッドレスブラウザで全ウェーブ・全難易度を自動プレイして調整した。
   STORY は標準的な編成で 7 ステージ到達可能、AI 対戦は EASY=勝ち越し、
   NORMAL=互角、HARD=かなり不利、という難度になっている。

5. **外部ライブラリの排除**
   Tailwind CDN の読み込みをやめ、CSS を自前で記述した
   （「ライブラリ・ビルドツール不使用」という開発方針に合わせるため）。
