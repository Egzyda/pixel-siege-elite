# CLAUDE.md — Pixel Siege: Elite 開発ガイドライン

## プロジェクト概要

**Pixel Siege: Elite** はスマホ縦画面向けのブラウザゲームです。
既存のタワーディフェンス的なリアルタイム配置ゲームを、**Mechabellum 風カジュアルオートバトラー**にリメイク中です。

- **技術スタック**: 純粋な HTML / CSS / Vanilla JavaScript（ライブラリ・ビルドツール不使用）
- **対象デバイス**: スマートフォン縦画面（PC でも縦長固定レイアウトで表示）
- **デプロイ先**: Cloudflare Pages（GitHub main ブランチ連携による自動デプロイ）
  - リポジトリ: `https://github.com/Egzyda/pixel-siege-elite`
  - main ブランチへ push すると Cloudflare Pages が自動でビルド・公開する

---

## 開発ルール（必須）

### コード編集後は必ず main にプッシュ

コードを変更・追加・修正した場合、**作業完了のたびに必ず以下を実行**してください。

```bash
git add -A
git commit -m "変更内容を簡潔に説明するコミットメッセージ"
git push origin main
```

push することで Cloudflare Pages が自動デプロイされ、実機（スマホ）で即時確認できます。

### 作業ブランチを使った場合は必ず main にマージする

作業ブランチ（`claude/xxx` など）で開発した場合、**作業完了のたびに必ず main にマージし、
main を origin へ push してください**。ユーザーから明示的な指示がなくても、これは常に実行する
デフォルト動作です。

```bash
git checkout main
git pull origin main
git merge --no-ff <作業ブランチ名> -m "Merge branch '<作業ブランチ名>' into main"
git push origin main
```

- 作業ブランチへの push だけでは Cloudflare Pages はデプロイされません。
  **main にマージして初めて自動デプロイが走ります。**
- コンフリクトが発生した場合は解決してからマージを完了させてください。
- マージ後、作業ブランチは削除せず残しておいて構いません（履歴として保持）。

### コメントは日本語で記述

コード内のすべてのコメントは日本語で記述してください。

```js
// 正しい例: 予算を消費してユニットを配置する
function placeUnit(unitKey, x, y) { ... }

// NG例: Place unit by spending budget
function placeUnit(unitKey, x, y) { ... }
```

### ブラウザ標準ダイアログ禁止

`alert()`, `confirm()`, `prompt()` は使用禁止。
代わりにトーストまたはモーダル（既存の `#modal` 要素や独自実装）を使用してください。

---

## アーキテクチャ

```
index.html    — 画面レイアウト、CSS、UI要素すべて（外部ライブラリ不使用）
sprites.js    — ドット絵スプライトデータ・カラーパレット・スプライト描画ユーティリティ
game-data.js  — ユニット定義(UNIT_DEFS)、予算テーブル、ウェーブ設定、ボス定義、
                強化・戦術定義、AI 難易度プリセット
game.js       — ゲームロジック本体（クラス・ループ・入力処理・UI 更新）
manifest.json — PWA マニフェスト（アイコン・テーマカラー設定）
sw.js         — Service Worker（ネットワーク優先＋オフライン時キャッシュ）
icons/        — PWA 用アイコン画像（knight スプライトから生成）
```

### 描画の共通ルール

スプライトは `drawSprite(ctx, sprite, pal, x, y, scale, opts)` で描画する。
`x, y` は**足元（スプライト下端）の中心**を指すアンカーで、影・HP バーの位置も
`getSpriteBox()` が返す実寸から計算する。左右反転は `opts.flipX` を使う
（上下反転はキャラクターが逆さまになるため使用しない）。

### ゲーム状態管理

`state` オブジェクト（`game.js`）が全ゲーム状態を保持します。
シーン（`state.scene`）で画面を管理:

| scene 値 | 意味 |
|---------|------|
| `'title'` | タイトル画面 |
| `'prep'` | 準備フェーズ（ユニット購入・配置） |
| `'battle'` | バトルフェーズ（全自動・観戦） |
| `'result'` | バトル結果表示 |

モード選択・ショップ・あそびかた・一時停止はシーンではなくオーバーレイとして表示する。
ランダム3択の報酬画面 (`'reward'`) は廃止し、準備フェーズのショップに統合済み。

### モード（`state.mode`）

| 値 | 内容 |
|----|------|
| `'story'` | 全7ステージ。ウェーブ＋ボス |
| `'survival'` | 配置上限なしで AI と無限に戦う。負けたら終了 |
| `'versus'` | 拠点とは別に体力を持つ対戦。先に相手の体力を 0 にした方が勝ち |

### UI レイアウトの原則

フィールド（`#stage`）の高さを最大化するため、下部パネルは**常にスリムなバー**に保つ。
ショップ・あそびかたは**ボトムシート**（`.sheet-backdrop`）、一時停止は中央モーダル
（`.modal-backdrop`）で表示する。パネルを縦に伸ばす実装は避けること。

`#app` には `padding-top: var(--safe-top)` を入れ、キャンバスがノッチに被らないようにする。

---

## 仕様概要（詳細は SPEC.md 参照）

- **縦画面固定**: 最大幅 `390px` の縦長エリアを中央表示（PC では左右が黒帯）
- **予算制**: マナ廃止。各ラウンド開始時に固定ゴールドを配布。購入のみに使用
- **準備フェーズ無制限**: 時間制限なし。「バトル開始」ボタンで開始
- **バトルは完全自動**: バトル中の操作は不可（観戦のみ。速度 x1/x2/x3 の切替のみ可能）
- **拠点オブジェクト化**: 拠点をドット絵の城として描画。攻撃されるとフラッシュ＋シェイク
- **ユニットショップ**: ランダム選択廃止。全ユニット常時一覧表示、予算で購入
- **3モード**: STORY（全7ステージ）・SURVIVAL（無限）・VERSUS（体力制対戦）

### 編成の引き継ぎとゴールドの使い道

- **ユニットは戦死しても編成から失われない**。次ラウンドは全員 HP 全回復で再配置される
  （毎ラウンド定額をもらう AI との資金差が開きすぎるのを防ぐため）
- **使わなかったゴールドは次ラウンドへ繰り越す**（`state.gold` に加算していく）
- 配置上限は `maxUnitsFor(mode)`。STORY / VERSUS は 15 体、SURVIVAL は実質無制限（安全上限 60）
- 準備フェーズでは配置済みユニットを**ドラッグで移動・タップで売却（全額返金）**できる
- **戦術**はバトル中に操作できないため、クールダウンごとに自動発動する
- ボス以外の全ユニット（goblin / orc / skeleton 含む）をプレイヤー・AI 双方が購入できる

### AI の対策編成

SURVIVAL / VERSUS では、AI は準備フェーズ開始時に **直前のラウンドのプレイヤー編成**を
`analyzeRoster()` で分析し、`AI_COUNTER_RULES` に従って刺さるユニットの抽選ウェイトを上げる
（`counterStrength` が 0 の EASY は対策しない）。前衛はプレイヤーの遠距離ユニットが
固まっている側に寄せて配置する。

---

## リメイク対応状況

- [x] ボスのドロップシャドウ位置がずれている → 足元アンカー方式に統一して解消
- [x] ボスのスプライト反転位置がおかしい → 上下反転を廃止し、敵は左右反転で表現
- [x] ボスパレットの色数不足（index 8〜10 が未定義で色化けしていた）を修正
- [x] PC 表示時にフィールドが横に広がりすぎる → `max-width: 390px` のレターボックス表示
- [x] マナ→予算システムへの完全刷新
- [x] 準備フェーズの追加（時間制限なし・購入・配置・売却）
- [x] 拠点のビジュアルオブジェクト化（城スプライト新規作成）
- [x] ユニットショップ（全ユニット常時表示・ステータス表示）
- [x] STORY モード（全7ステージ）／ BATTLE モード（AI 対戦・難易度3種）
- [x] PWA マニフェスト・アイコン・Service Worker

### 追加実装（v1.1）

- [x] ユニットが戦死しても編成に残る（毎ラウンド全回復で再配置）
- [x] 予算の繰り越し
- [x] ノッチ回避（`#app` の safe-area padding）
- [x] ショップをボトムシート化してフィールドを約 1.4 倍に拡大
- [x] 全ユニットをプレイヤー・AI 双方が購入可能に
- [x] SURVIVAL モード（配置上限なし・無限ラウンド）
- [x] VERSUS モード（体力制・メカベラム方式のラウンドダメージ）
- [x] AI がプレイヤー編成に対策してくる（2ラウンド目以降）
- [x] 戦術のクールダウン表示（残り秒数つきチップ）
- [x] 一時停止（再開 / やり直し / タイトルへ）
- [x] プレイ記録とクリアマーク
- [x] あそびかたモーダル・タイトル画面のドット絵アニメ
- [x] 最新版に更新するボタン

### 追加実装（v1.2）

- [x] ショップのタブ切替でシートの高さが変わらないよう固定化
- [x] goblin / orc / skeleton に個性を付与（安さと物量 / 薙ぎ払い範囲攻撃 / 超長射程）
- [x] 「全解除」を廃止し、1手ずつ戻せるアンドゥに変更（AI の対策編成を無意味にしないため）
- [x] 防衛ユニットが全滅した拠点は急速に崩壊する（間延びしたちまちま攻城を防ぐ）
- [x] ユニット名・ステータス表示・ボス名を日本語化
- [x] VERSUS の体力減少に「一呼吸置いてから減る」演出を追加
- [x] STORY: 最初のウェーブをバトル開始と同時に配置、待機中は前進せず静止
- [x] フィールドの左右を実質的な壁として扱い、ノックバックで画面外に出ないように
- [x] SURVIVAL / VERSUS 限定のエリートユニット4種（ボス風スプライトを縮小流用）
- [x] 拠点が射程内の敵ユニットへ弱い援護射撃を行うように（弾が見えて分かりやすい）

### 今後の課題（任意）

- [ ] BGM / 効果音
- [ ] リプレイやスコアランキング

---

## Cloudflare Pages 設定メモ

- ビルドコマンド: なし（静的ファイルをそのまま配信）
- ビルド出力ディレクトリ: `/`（ルートディレクトリ）
- 環境変数: 不要
- main ブランチへの push で自動デプロイが走る

---

## PWA 対応

- `manifest.json` を `index.html` から読み込む
- アイコン: Knight（歩兵）スプライトをドット絵で描画して PNG 生成（`icons/` に配置）
- テーマカラー: `#0f172a`（ダークネイビー）
