# CLAUDE.md — Pixel Siege: Elite 開発ガイドライン

## プロジェクト概要

**Pixel Siege: Elite** はスマホ縦画面向けのブラウザゲームです。
既存のタワーディフェンス的なリアルタイム配置ゲームを、**Mechabellum 風カジュアルオートバトラー**にリメイク中です。

- **技術スタック**: 純粋な HTML / CSS / Vanilla JavaScript（ライブラリ・ビルドツール不使用）
- **対象デバイス**: スマートフォン縦画面（PC でも縦長固定レイアウトで表示）
- **デプロイ先**: Cloudflare Pages（GitHub main ブランチ連携による自動デプロイ）
  - リポジトリ: `https://github.com/Egzyda/pixel-siege-elite`
  - main ブランチへ push すると Cloudflare Pages が自動でビルド・公開する

---

## 開発ルール（必須）

### チャットでの返答は日本語で行う

ユーザーとのやり取り（チャット上の返答・説明・確認メッセージなど）は**すべて日本語**で行うこと。
コード内コメントを日本語で書くルール（後述）とは別に、会話そのものも常に日本語とする。

### ゲームバランス・根幹システムに関わる変更は事前に相談する

以下に該当する変更は、**実装前に必ずユーザーに相談し、方向性の合意を得てから着手**すること。
「バランスを直して」のような大枠の依頼だけでは、具体的な実装方針（例: どのモードの
どの仕組みをどう変えるか）まで一任されたとはみなさない。複数の実装案が考えられ、
トレードオフがある変更を、一つに決めて黙って進めてはいけない。

- ゲームモードの設計思想・勝利条件・難易度モデルなど、根幹の仕組みに関わる変更
- 既存の仕様書（本ファイル・SPEC.md）に書かれた設計意図と矛盾しうる変更

【経緯・具体例】STORY モードは「固定された台本（ウェーブ・ボス）をどう攻略するか」を
楽しさの核とする設計であり、SURVIVAL / VERSUS とは異なり、プレイヤーの育成度に応じて
敵を動的にスケーリングする「適応難易度」は**導入しない**。過去にストーリーの温存化
（ナイト単騎編成でクリアできてしまう問題）への対処として適応難易度を試験導入したが、
「決まった敵をどう攻略するかがストーリーの楽しさなのに、これでは他モードの劣化版に
なってしまう」とユーザーから指摘があり撤回した。STORY の難易度調整は、あくまで
固定ウェーブ側の数値・敵構成や、経済側（予算・強化上限）の調整で行う。

### コード編集後は必ず main にプッシュ

コードを変更・追加・修正した場合、**作業完了のたびに必ず以下を実行**してください。

```bash
git add -A
git commit -m "変更内容を簡潔に説明するコミットメッセージ"
git push origin main
```

push することで Cloudflare Pages が自動デプロイされ、実機（スマホ）で即時確認できます。

### 作業ブランチを使った場合は必ず main にマージする

作業ブランチ（`claude/xxx` など）で開発した場合、**作業完了のたびに必ず main にマージし、
main を origin へ push してください**。ユーザーから明示的な指示がなくても、これは常に実行する
デフォルト動作です。

```bash
git checkout main
git pull origin main
git merge --no-ff <作業ブランチ名> -m "Merge branch '<作業ブランチ名>' into main"
git push origin main
```

- 作業ブランチへの push だけでは Cloudflare Pages はデプロイされません。
  **main にマージして初めて自動デプロイが走ります。**
- コンフリクトが発生した場合は解決してからマージを完了させてください。
- マージ後、作業ブランチは削除せず残しておいて構いません（履歴として保持）。

### コメントは日本語で記述

コード内のすべてのコメントは日本語で記述してください。

```js
// 正しい例: 予算を消費してユニットを配置する
function placeUnit(unitKey, x, y) { ... }

// NG例: Place unit by spending budget
function placeUnit(unitKey, x, y) { ... }
```

### ブラウザ標準ダイアログ禁止

`alert()`, `confirm()`, `prompt()` は使用禁止。
代わりにトーストまたはモーダル（既存の `#modal` 要素や独自実装）を使用してください。

---

## アーキテクチャ

```
index.html    — 画面レイアウト、CSS、UI要素すべて（外部ライブラリ不使用）
sprites.js    — ドット絵スプライトデータ・カラーパレット・スプライト描画ユーティリティ
game-data.js  — ユニット定義(UNIT_DEFS)、予算テーブル、ウェーブ設定、ボス定義、
                強化・戦術定義、AI 難易度プリセット
game.js       — ゲームロジック本体（クラス・ループ・入力処理・UI 更新）
manifest.json — PWA マニフェスト（アイコン・テーマカラー設定）
sw.js         — Service Worker（ネットワーク優先＋オフライン時キャッシュ）
icons/        — PWA 用アイコン画像（knight スプライトから生成）
```

### 描画の共通ルール

スプライトは `drawSprite(ctx, sprite, pal, x, y, scale, opts)` で描画する。
`x, y` は**足元（スプライト下端）の中心**を指すアンカーで、影・HP バーの位置も
`getSpriteBox()` が返す実寸から計算する。左右反転は `opts.flipX` を使う
（上下反転はキャラクターが逆さまになるため使用しない）。

### ゲーム状態管理

`state` オブジェクト（`game.js`）が全ゲーム状態を保持します。
シーン（`state.scene`）で画面を管理:

| scene 値 | 意味 |
|---------|------|
| `'title'` | タイトル画面 |
| `'prep'` | 準備フェーズ（ユニット購入・配置） |
| `'battle'` | バトルフェーズ（全自動・観戦） |
| `'result'` | バトル結果表示 |

モード選択・ショップ・あそびかた・一時停止はシーンではなくオーバーレイとして表示する。
ランダム3択の報酬画面 (`'reward'`) は廃止し、準備フェーズのショップに統合済み。

### モード（`state.mode`）

| 値 | 内容 |
|----|------|
| `'story'` | 全10ステージ。ウェーブ＋ボス |
| `'survival'` | 全10ステージのクリア制。VERSUSと同じ体力制で、負けても体力が残っていれば続行できるが、最終ステージだけは実際に勝たないとクリアにならない |
| `'versus'` | 拠点とは別に体力を持つ対戦。先に相手の体力を 0 にした方が勝ち |

### UI レイアウトの原則

フィールド（`#stage`）の高さを最大化するため、下部パネルは**常にスリムなバー**に保つ。
ショップ・あそびかたは**ボトムシート**（`.sheet-backdrop`）、一時停止は中央モーダル
（`.modal-backdrop`）で表示する。パネルを縦に伸ばす実装は避けること。

`#app` には `padding-top: var(--safe-top)` を入れ、キャンバスがノッチに被らないようにする。

---

## 仕様概要（詳細は SPEC.md 参照）

- **縦画面固定**: 最大幅 `390px` の縦長エリアを中央表示（PC では左右が黒帯）
- **予算制**: マナ廃止。各ラウンド開始時に固定ゴールドを配布。購入のみに使用
- **準備フェーズ無制限**: 時間制限なし。「バトル開始」ボタンで開始
- **バトルは完全自動**: バトル中の操作は不可（観戦のみ。速度 x1/x2/x3 の切替のみ可能）
- **拠点オブジェクト化**: 拠点をドット絵の城として描画。攻撃されるとフラッシュ＋シェイク
- **ユニットショップ**: ランダム選択廃止。全ユニット常時一覧表示、予算で購入
- **3モード**: STORY（全10ステージ）・SURVIVAL（全10ステージのクリア制）・VERSUS（体力制対戦）

### 編成の引き継ぎとゴールドの使い道

- **ユニットは戦死しても編成から失われない**。次ラウンドは全員 HP 全回復で再配置される
  （毎ラウンド定額をもらう AI との資金差が開きすぎるのを防ぐため）
- **使わなかったゴールドは次ラウンドへ繰り越す**（`state.gold` に加算していく）
- 配置上限は `maxUnitsFor(mode)`。STORY / VERSUS は 15 体、SURVIVAL は実質無制限（安全上限 60）
- 準備フェーズでは配置済みユニットを**ドラッグで移動・タップで売却（全額返金）**できる
- **戦術**はバトル中に操作できないため、クールダウンごとに自動発動する
- ボス以外の全ユニット（goblin / orc / skeleton 含む）をプレイヤー・AI 双方が購入できる
- SURVIVAL / VERSUS では **エリートユニット4種**（`ELITE_UNITS`）も購入できる

### ユニット設計の指針

- **ノックバックは重量級ユニットの個性**として扱う（ゴーレム・ウォーロード）。
  ナイトのような「並べて壁にする」ユニットに持たせると、敵を押し返してしまい
  壁として機能しなくなるため付けない
- ステータス表示では「近接/遠距離」と「範囲攻撃の有無」は別の性質として扱う
  （`reachLabel()` / `splashRadius()`）。メイジは遠距離かつ範囲、オークは近接かつ範囲

### AI の対策編成

SURVIVAL / VERSUS では、AI は準備フェーズ開始時に **直前のラウンドのプレイヤー編成**を
`analyzeRoster()` で分析し、`AI_COUNTER_RULES` に従って刺さるユニットの抽選ウェイトを上げる
（`counterStrength` が 0 の EASY は対策しない）。前衛はプレイヤーの遠距離ユニットが
固まっている側に寄せて配置する。

---

## リメイク対応状況

- [x] ボスのドロップシャドウ位置がずれている → 足元アンカー方式に統一して解消
- [x] ボスのスプライト反転位置がおかしい → 上下反転を廃止し、敵は左右反転で表現
- [x] ボスパレットの色数不足（index 8〜10 が未定義で色化けしていた）を修正
- [x] PC 表示時にフィールドが横に広がりすぎる → `max-width: 390px` のレターボックス表示
- [x] マナ→予算システムへの完全刷新
- [x] 準備フェーズの追加（時間制限なし・購入・配置・売却）
- [x] 拠点のビジュアルオブジェクト化（城スプライト新規作成）
- [x] ユニットショップ（全ユニット常時表示・ステータス表示）
- [x] STORY モード（全7ステージ）／ BATTLE モード（AI 対戦・難易度3種）
- [x] PWA マニフェスト・アイコン・Service Worker

### 追加実装（v1.1）

- [x] ユニットが戦死しても編成に残る（毎ラウンド全回復で再配置）
- [x] 予算の繰り越し
- [x] ノッチ回避（`#app` の safe-area padding）
- [x] ショップをボトムシート化してフィールドを約 1.4 倍に拡大
- [x] 全ユニットをプレイヤー・AI 双方が購入可能に
- [x] SURVIVAL モード（配置上限なし・無限ラウンド）
- [x] VERSUS モード（体力制・メカベラム方式のラウンドダメージ）
- [x] AI がプレイヤー編成に対策してくる（2ラウンド目以降）
- [x] 戦術のクールダウン表示（残り秒数つきチップ）
- [x] 一時停止（再開 / やり直し / タイトルへ）
- [x] プレイ記録とクリアマーク
- [x] あそびかたモーダル・タイトル画面のドット絵アニメ
- [x] 最新版に更新するボタン

### 追加実装（v1.2）

- [x] ショップのタブ切替でシートの高さが変わらないよう固定化
- [x] goblin / orc / skeleton に個性を付与（安さと物量 / 薙ぎ払い範囲攻撃 / 超長射程）
- [x] 「全解除」を廃止し、1手ずつ戻せるアンドゥに変更（AI の対策編成を無意味にしないため）
- [x] 防衛ユニットが全滅した拠点は急速に崩壊する（間延びしたちまちま攻城を防ぐ）
- [x] ユニット名・ステータス表示・ボス名を日本語化
- [x] VERSUS の体力減少に「一呼吸置いてから減る」演出を追加
- [x] STORY: 最初のウェーブをバトル開始と同時に配置、待機中は前進せず静止
- [x] フィールドの左右を実質的な壁として扱い、ノックバックで画面外に出ないように
- [x] SURVIVAL / VERSUS 限定のエリートユニット4種（ボス風スプライトを縮小流用）
- [x] 拠点が射程内の敵ユニットへ弱い援護射撃を行うように（弾が見えて分かりやすい）

### 追加実装（v1.3）

- [x] 相打ちで両軍全滅した場合、時間切れを待たず即決着（STORY は味方全滅で即敗北）
- [x] ステータス表示で「近接/遠距離」と「範囲攻撃」を別項目にし、範囲は半径を明記
- [x] 拠点破壊 → 体力ゲージへ衝撃弾が飛ぶ演出（因果が目で追えるように）
- [x] ナイトのノックバックを廃止（壁役として機能させる）。ノックバックは重量級へ集約
- [x] リッチのドレインに回復ポップアップを追加（発動が見えていなかった）
- [x] フィールドのユニットをタップすると説明カードを表示（陣営カラーで描画）
- [x] タイトルにユニット図鑑を追加

### 追加実装（v1.4）

- [x] ユニット種別ごとのレベルアップ（`unitLevelCost()` / `unitLevelMult()`）。安価・少しずつ強化
- [x] 全体強化（強化タブ）の価格を緩和（`UPGRADE_PRICE_SCALE` 1.6→1.18、効果も控えめに）
- [x] 自ユニットのタップは即売却ではなく説明カード＋売却ボタンに変更（配置換え放題を防止）
- [x] ノックバックに上限（`KNOCKBACK_CAP`）を設け、「軽い後退」程度に統一

### 追加実装（v1.5）

- [x] VERSUS の配置上限を SURVIVAL と同じ実質無制限に変更（`maxUnitsFor()`）。STORY は 15 体のまま据え置き
- [x] ゴーレムの攻撃力を調整（dmg 75→58）。耐久特化の盾役として火力は控えめに
- [x] ゴブリンの効率を改善（dmg 9→11・rate 38→32）。数を並べた時の総火力がナイトを上回る物量アーキタイプとして機能するよう強化
- [x] ユニットカードのLvバッジを未強化時から「Lv.1」として常時表示し、強化で出現する際のレイアウトずれ・バッジ内改行を解消（`.card-sub` をバッジ単位で折り返すように変更）

### 追加実装（v1.6）

- [x] ユニット個別レベルアップ・全体強化の効果を大幅増強。「そのユニット1体分の金額(=購入価格)を注ぎ込むとステータスがおよそ2倍になる」を基準に再設計し、価格の伸び率と効果の伸び率をそろえることで、どのタイミングで注ぎ込んでも金額に見合う効果を保つようにした（`UNIT_LEVEL_STAT_GAIN` 5%→15%・コスト計算を複利式に変更、`atk_boost`等の全体強化も8〜10%→14〜20%に増強、`UPGRADE_PRICE_SCALE` 1.18→1.12）
- [x] 攻撃対象のロックオン制を導入。一度狙った相手は、死ぬか大きく引き離される（`TARGET_LOCK_RANGE`）まで狙い続けるようにし、より近い敵が新しく現れても目移りしなくなった。タンクやノックバックで足止めする意味が保たれる（回復役は従来通り毎フレーム最も負傷した味方を選び直す）

### 追加実装（v1.7）

- [x] ユニット個別レベル・全体強化(複利で伸びるもの)に上限を追加。v1.6 で価格と効果の伸び率をそろえた結果、1種に注ぎ込み続けると際限なく強くなってしまっていた（例: ゴブリンLv30で約66倍）のを、`UNIT_LEVEL_MULT_CAP`（ユニット個別、4倍）と `ATK_MULT_CAP` / `HP_MULT_CAP` / `RATE_MULT_MIN` / `MOVE_MULT_CAP` / `RANGE_MULT_CAP`（全体強化）で頭打ちにした。上限到達後は購入してもゴールドが減らず、ショップにも「★ 最大まで強化済み」と表示される
- [x] ショップシート左下にもアンドゥボタンを追加（`#btn-undo-shop`）。準備フェーズ下部バーのボタンと同じ `undoLastAction()` を呼び、状態も連動して更新される
- [x] STORY で配置上限(15体)に達した際、ショップのユニットカードが購入部分(アイコン/ステータス)だけ薄くなり、強化欄(強化する ボタン)は薄くならないよう修正。配置上限とレベルアップ可否は無関係なため、強化まで操作不可に見えていた誤解を解消（`.card-buyable` でDOMを分離）

### 追加実装（v1.8）

- [x] フィールドで自ユニットをタップした際の売却ボタンを「今ラウンド購入分のみ」に限定。前ラウンド以前から編成にいるユニットは売却できず、代わりに説明を表示する（ユニットに `boughtRound` を記録）
- [x] 敵ユニットのタップ説明カードにも Lv バッジを表示（敵はレベルアップしないため常に「Lv.1」固定表示）
- [x] SURVIVAL の AI 編成を「メカベラム方式」に変更。前ラウンドの編成を引き継がず、その時点で持っているはずの資金全額で毎ラウンド新しく組み直す（`buildAiRoster()` 冒頭で編成を全額返金してから再構築）。プレイヤーと全く同じ条件（編成・資金を引き継ぐ）を保つのは VERSUS のみとし、VERSUS の挙動は変更していない
- [x] AI の対策編成にバリエーションを追加。①試合開始時に「物量型/重装型/遠距離型/バランス型」から1つ AI の「好み」(`AI_PERSONALITIES`) を抽選し、対策とは独立して常時ウェイトに乗せる（開始時にヒント表示、EASYは対象外）。②対策発動時の補正強度にブレを持たせ、条件を満たしても一定確率で見送るようにした（`AI_COUNTER_VARIANCE`）。HARD は見送り率を低く抑えて対策の信頼性（弱いプレイにしない）を優先しつつ、NORMAL 以下はよりばらつきが出るようにしてある

### 追加実装（v1.9）

- [x] ユニット個別レベルの抜け道を修正。v1.6〜v1.7 は「そのユニット1体分の金額で2倍相当」を単体の購入価格だけを基準に設計していたため、複数体所持している状態で強化すると全員に効果が及び、「安く強化してから量産する」ことで初期予算（例: 80G）でも通常の3〜4倍お得にステータス2倍のユニットを量産できてしまっていた（例: ナイト3体を80Gで2倍化）。
  - `unitLevelCost()` にユニットの所持数(count)を追加し、価格が所持数にも比例するようにした（3体持っていれば3体分の価格）
  - 新たに `unitBuyCost()` を導入。ユニット種のレベルが上がっているほど新規購入価格も比例して上がるようにした（＝「安いうちに1体だけ強化してから量産する」を購入価格側からも塞いだ）
  - 上記2つの組み合わせにより、強化を先にしても後にしても「性能等価1体あたりの金額」が素のユニットを購入する場合とほぼ同額（差は数%程度）になるよう修正。売却時の返金額も現在の実勢価格（強化を反映した額）になる
  - AI（敵ユニット）はプレイヤーの `unitLevels` の影響を受けないため、この変更による影響はない

### 追加実装（v2.0）

- [x] 準備フェーズ（購入フェーズ）の下部バーにもタイトルへ戻るボタン（🏠）を追加。従来は一時停止モーダル（バトル中のみ）からしかタイトルに戻れなかった
- [x] VERSUS 限定で、AI もユニット個別レベルアップに投資してくるように変更。価格修正後も「数を増やすよりレベルアップの方が圧倒的に強い」状態は残っていた（少数精鋭がタンク性能・的の少なさの面で有利なため）ため、プレイヤーだけが使える手にしておくのは不公平と判断
  - 対象ユニットは「現在の編成で最も多い種類」（＝主力）。「好み」があれば同ウェイトで優先
  - 難易度ごとに `levelInvestRatio`（毎ラウンド収入のうちレベルアップに回す割合）を設定: EASY=0（投資しない）/ NORMAL=0.2 / HARD=0.35
  - 計算式・上限（`UNIT_LEVEL_MULT_CAP`）・「安いうちに強化してから量産」対策（`aiUnitBuyCost()`）はプレイヤー側と共通のロジックを再利用し、`state.aiUnitLevels` に独立して記録（プレイヤーの `unitLevels` とは無関係）
  - 敵ユニットの説明カードの Lv 表示も、VERSUS では AI の実際の投資状況を反映するように変更（それ以外のモードは従来通り常に Lv.1）

### 追加実装（v2.1）

- [x] 守護天使（戦術）を習得していても、クールダウン待ちで盤面に不在の瞬間に味方ユニットが0体だと拠点が自動崩壊してしまうバグを修正。`updateSiegeCollapse()` が `hasPendingSummon()`（召喚系戦術が控えているか）を見ていなかったのが原因。`checkBattleEnd()` の即敗北判定は元々これを考慮していたため、崩壊判定側も揃えた
- [x] スケルトンに「命中した相手を鈍足にする」個性を追加。低火力・低耐久な代わりに超長射程だけが取り柄で存在感が薄かったため、矢を受けた相手の移動速度を3秒間半減させる効果を付与し、足止め役としての役割を持たせた（`SKELETON_SLOW_MULT` / `SKELETON_SLOW_DURATION`）

### 追加実装（v2.2）

- [x] ユニット個別レベルと全体強化(攻撃/HP)の「合計倍率」に上限を追加。ナイト・アーチャーを3体ずつだけ編成し、両方の強化を全振りするだけでSTORY(全7ステージ、15体編成前提)を危なげなくクリアできてしまう報告を受け、`node`でのシミュレーションで再現・切り分けを行った結果、片方の系統だけでも複利で伸びる上限（ユニット個別4倍・全体強化2〜2.2倍）同士が掛け算で効くと、少数精鋭に全投資するだけで最大8〜8.8倍のステータスに達してしまうことが判明。`PLAYER_ATK_TOTAL_CAP` / `PLAYER_HP_TOTAL_CAP`（ともに2.5倍）で「atkMult() × unitLevelMult(key)」「hpMult() × unitLevelMult(key)」自体の上限を設け、片方に全振りしても両方に分散させても最終的な強さの天井が変わらないようにした。全体強化を積んでいるほどユニット個別レベルの「実効上限」が下がるため、`buyUnitLevel()` の上限判定・ショップの「★最大まで強化済み」表示・カードのステータスプレビューもすべてこの実効上限を見るように統一（`effectiveUnitLevelCap()`）。通常の15体に分散させるプレイでは上限に引っかからず、AI(VERSUS)は全体強化を持たないため影響を受けない
- [x] ついでにユニット購入価格(`unitBuyCost()`)も、ユニット個別レベルだけでなく全体強化(攻撃/HPの幾何平均 `globalPowerMult()`)を織り込むように修正。そうしないと「全体強化を先に安く積んでから新規ユニットを買い増す」という、個別レベル版と同種の抜け道が全体強化側に残ってしまうため

### 追加実装（v2.3）

- [x] ショップカードのコスト表示(`.card-cost`)にmin-widthを設定。レベルアップで購入価格が2桁→3桁に増えると`.card-id`の幅が圧迫され、隣接する所持数(×N)表示が一段折り返されてカードの高さがずれる不具合があった（セントリーLv2で発生）。コストの表示幅を固定してこのズレを解消
- [x] ユニット説明文から「ナイトより」等の他ユニットとの比較表現を削除し、各ユニット単体で完結する説明にした
- [x] ユニット図鑑のカテゴリ分け(基本ユニット/エリートユニット/戦術で召喚)を廃止し、全ユニットを同列の1グループとして表示するよう変更(`ELITE_UNITS`はショップ購入可否には既に影響しない表示上の区分だったため、表示側の整理のみで完結)
- [x] ロード(graveLord)を全面リメイク。リッチ(lich)と同じ`boss_skeleton`形状を流用していたため見た目がほぼ同一だった問題を、wizard形状+ネクロ配色への変更で解消。近接lifestealを廃止し、移動速度0・射程70(かなり短め)の遠距離ユニットに再設計。倒れた味方がいる間だけ一定間隔で1体ずつ蘇生する新能力`reviveAlly`を追加(`state.playerCorpses`に戦死した味方の種類を記録し、初回は即時発動、以降は`reviveInterval`のクールタイムを置く。倒れた味方がいなければ何もしない)
- [x] STORYモードは難易度(EASY/NORMAL/HARD)がそもそも何にも影響しない仕様のため、STORY選択中はモード選択画面の難易度セレクタごと非表示にした(`syncModeSelectExtras()`。SURVIVAL/VERSUSでは引き続き表示)
- [x] **STORY EXTRA**を追加。通常STORYを1回全クリアすると、モード選択画面のSTORYカード下にEXTRAトグルが解禁される、NG+的な高難度周回モード。過去に撤回した「プレイヤーの育成度を見て動的に敵を強くする適応難易度」ではなく、固定ウェーブ側の敵数(`STORY_EXTRA_ENEMY_MULT`=1.15倍)・ボスのHP/攻撃力(`STORY_EXTRA_BOSS_MULT`=1.25倍)をあらかじめ底上げした「もう1本の固定台本」(`STORY_STAGES_EXTRA` / `BOSS_DEFS_EXTRA`、`currentStoryStages()` / `currentBossDefs()`で解決)として用意し、プレイヤーの状態は一切参照しない。予算は当初0.9倍に絞る案で実装したが、「プレイヤーの自由度を削る形の難易度調整はしない」とユーザーから指摘があり撤回。**予算はSTORY通常と完全に共通**(`budgetForRound()`はstoryExtraを見ない)とし、難易度は敵側の強化のみで担う。`state.storyExtra`でON/OFFを保持し、セーブ/ロードにも対応。プレイ記録にも`extraCleared` / `extraBestWave` / `extraClearCount`を独立して記録し、通常クリアの記録とは混ざらないようにした
- [x] インプに「接敵(射程内到達)するまで透明化しており、敵から狙われない」個性を追加(`stealthUntilEngage`)。無敵ではないため、直接攻撃や範囲攻撃が偶然命中すればダメージは通常通り受ける。ユニット・拠点・ボスいずれの索敵ロジックからも透明化中は対象から除外し、射程内に踏み込んだ瞬間に解除(以後そのバトル中は解除されたまま)。観戦側にも分かるよう、透明化中は半透明で描画する

### 追加実装（v2.4）

- [x] 敵(AI)側のロードが蘇生しないバグを修正。蘇生ロジックが`this.isP`と`state.playerCorpses`に決め打ちされており、AI購入分のロードは常に「味方が誰も倒れていない」扱いになっていた。`state.enemyCorpses`を新設し(`onUnitDeath()`で`!isP`側の戦死を記録、`deployRoster()`でリセット)、`Unit.update()`の蘇生処理を`this.isP`に応じてどちらの配列を見るか・どちらの陣営で蘇生するかを汎用化した
- [x] VERSUSでAIの初期資金がプレイヤーと食い違っていたバグを修正。`buildAiRoster()`の難易度別資金補正(`budgetMult`によるramp)がSURVIVALだけでなくVERSUSにも掛かっており、「同じ条件のゲーム」という既存の設計意図に反していた。ramp計算を`state.mode === 'survival'`限定にし、VERSUSは常に`ramp = 1`(=プレイヤーと完全に同額)に固定
- [x] ボス4「ゴブリン大王」を「ゴブリンキング」に改名
- [x] ラスボス(カオスタイタン)を強化。第1形態から広範囲の薙ぎ払い(`meleeSplash:150, meleeSplashRate:0.35`)を追加し、HP66%以下(第2形態)に入った瞬間に一度だけ「覚醒」。味方全員を壁際まで大きくノックバックさせたうえで、以降は長射程の範囲攻撃タイプ(`awaken:{range,splash,dmgMult}`)に永続的に切り替わる。素の`dmg`をそのまま全員に当てると近接薙ぎ払いより火力が上がってしまうため`dmgMult:0.4`で覚醒後AOEの威力を調整し、Playwrightでの複数回の10ステージ通しプレイで「勝てるが際どい」バランスになるよう調整した
- [x] ユニット図鑑(タイトル画面のプレビュー)でのコスト表示バグを修正。プレビューレベルに応じた価格ではなく、実際のセーブデータの`state.unitLevels`(前回プレイの値)を使った`unitBuyCost()`を表示してしまっていたため、プレビュー中のレベルアップ/ダウンに価格が連動していなかった。プレビューモード時は`def.cost * lvlMult`を計算して表示するよう修正
- [x] オークの範囲攻撃半径を拡大(`meleeSplash` 36→46)
- [x] ウォーロードを「低頻度・重量級」に全面再設計。攻撃間隔を70→180に伸ばす代わりに攻撃力を45→260に引き上げ、同レベルのナイトなら一撃で沈める重さにした。ノックバックも通常の`KNOCKBACK_CAP`(4)を超えられる専用の`knockbackCap:12`を追加してより大きく吹き飛ぶように変更。攻撃直前(`CHARGE_TELL_FRAMES`=45フレーム以内)は`chargeAttack`フラグにより溜めの火花エフェクトが出るようにし、力を溜めていることが見た目で分かるようにした
- [x] リッチを「吸収回復(lifesteal)」役から「周囲の味方の攻撃力を底上げする支援型」に全面リメイク。`auraRadius`(効果半径)・`auraBuff`(倍率1.25倍)を追加し、同じ半径内に複数のリッチがいても倍率は重複せず最大値のみ採用(`Unit.update()`で毎フレーム`auraMult`を再計算)。回復役の回復量には影響しない。自身の遠距離攻撃は`dmg`を大幅に下げて弱めに据え置き。効果範囲は発動中だけでなく常に半透明の円で表示する(`Unit.draw()`)

### 追加実装（v2.5）— SURVIVALをメカベラム風クリア制に全面再設計

既存の「配置上限なしでAIと無限に戦い、負けたら終了」という無限SURVIVALを廃止し、
Mechabellumのサバイバルを参考にした**全10ステージのクリア制**に全面的に作り直した。
ユーザーからの明示的な依頼（"既存の無限サバイバルは削除。基本はバーサス方式ですが、
ラストステージは勝たないとクリアにならないシステムで"）に基づく、根幹の勝利条件・
難易度モデルに関わる変更のため、実装前に方向性を確認した上で着手している。

- [x] **体力の持ち越し**: VERSUSと全く同じ`playerLife`/`aiLife`の仕組みを再利用。ラウンドに
  負けても体力が残っていれば試合は続行し、拠点は毎ラウンド全回復して次のステージへ進む
- [x] **最終ステージ(`SURVIVAL_STAGES`=10)だけは実際に勝たないとクリアにならない**。
  通常のVERSUSは「どちらかの体力が0になった時点で決着」だが、SURVIVALはそれだと
  最終ステージより前に相手の体力を削り切って早期終了できてしまうため、`endBattle()`で
  「体力0になった場合」と「最終ステージの決着がついた場合」だけを終了条件とし、
  最終ステージで負けた（体力が残っていても）場合は`survivalEndReason:'finalStageFailed'`
  としてクリア扱いにしない。逆に相手の体力を最終ステージより前に0にしても、以降の
  ステージでは単に0のまま据え置かれるだけで試合は終わらない
- [x] **専用の予算テーブル**(`survivalBudgetForRound()`)を新設。開始資金300G、以降1ステージ
  ごとに+50G(`SURVIVAL_START_GOLD` / `SURVIVAL_BUDGET_STEP`)。VERSUSの`budgetForRound()`
  とは完全に独立させ、STORY/VERSUSの経済に影響を与えない
- [x] **難易度によるAI資金の意図的な非対称**(`SURVIVAL_AI_BUDGET_MULT`)。VERSUSは
  「プレイヤーと全く同じ条件」を守る必要があるため資金の水増しを一切しない設計のままだが、
  SURVIVALはユーザーの要望通り EASY(0.85倍・プレイヤーより少なめ) / NORMAL(1.0倍・互角) /
  HARD(1.2倍・プレイヤーより格上) とし、「配置は全部ガチ」という要望に沿ってラウンド経過
  による緩和は行わず初手からその倍率で来る。AI編成の組み方自体は既存の「メカベラム方式」
  (`buildAiRoster()`。前ラウンドの編成を全額返金してから資金全額で毎ラウンド新しく組み直す)
  と直前のプレイヤー編成への対策(`counteredPool()`)をそのまま流用しており、
  「毎回相手はこちらに合わせて盤面を組んでくる」という要望は元々ある仕組みで満たされる
- [x] **敵専用の精鋭ユニット`eliteGuard`**(「重装親衛兵」)を新設。5ステージ目以降
  (`SURVIVAL_ELITE_UNLOCK_STAGE`)、AIの購入プールに低めのウェイトで混じるようになる。
  ショップの`SHOP_UNITS`/`ELITE_UNITS`どちらにも含めないことでプレイヤーは購入できない
  敵専用ユニットとした。性能はユーザー例示の「ナイトが大型化してステータス上限まで
  強化しきったのと同等」をそのまま採用し、ナイトのHP・攻撃力を`UNIT_LEVEL_MULT_CAP`
  (4倍)した値、価格は「プレイヤーが`unitLevelCost()`で同じ倍率まで課金した場合の
  実勢コスト」から逆算(結果85G)することで、専用ユニットだからと経済を無視した
  強さにならないよう調整した
- [x] 体力ゲージHUD(`updateVersusHud()`)・上部レイアウトの体力バー分の余白(`topInset()`)を
  VERSUS限定からSURVIVALにも拡張。結果画面・HUD表示・プレイ記録もSURVIVAL用に全面更新し、
  記録は`{cleared, bestStage, clearCount}`を難易度ごとに持つ形にした(旧仕様の
  「難易度ごとの単なる到達ラウンド数値」とは別物のため、旧セーブの数値データは互換を
  取らず初期値から開始する)

### 追加実装（v2.6）

- [x] モード選択画面のレイアウト崩れを修正。STORYの「EXTRA切替」は STORY カードの直下、
  SURVIVAL/VERSUS の「難易度セレクタ」は3枚のモードカードの後ろとバラバラの位置に
  あったため、モードを切り替えるたびにレイアウトが左右非対称にガタつく問題があった。
  両方とも「3枚のモードカードの直後」という同じ位置に統一し、選んだモードによらず
  切替UIが同じ場所に出るようにした
- [x] SURVIVALの敵専用精鋭ユニットを`eliteGuard`(ナイト系)1種類だけから3種類に拡充。
  同じ顔ぶれの繰り返しでプレイヤーを飽きさせないよう、`makeSurvivalElite(baseKey, ...)`
  という共通ヘルパーを新設し、ベースにするユニットの性質(射程・範囲攻撃の有無など)を
  そのまま引き継いだ上でステータスだけ底上げする方式にした。`eliteArcher`(アーチャー系・
  遠距離)、`eliteMage`(メイジ系・範囲攻撃)を追加し、近接(`eliteGuard`)と合わせて
  近接/遠距離/範囲攻撃の3タイプが揃うようにした。価格は既存の`eliteGuard`と同じロジック
  (`unitLevelCost()`ベースの実勢コスト逆算)をそのまま再利用しているため、専用ユニットの
  経済バランスの考え方は変えていない

### 追加実装（v2.7）

- [x] STORYの勝利条件を変更。ボスを撃破しただけでは勝利にせず、残っている雑魚を全て
  倒し切って初めて勝利になるようにした(`checkBattleEnd()`)。ボス撃破直後の演出
  (`bossClearDelay`の余韻)はそのまま残し、演出が終わった時点で敵ユニットが1体も
  残っていなければ勝利、残っていれば通常のバトルを継続する
- [x] 全モード共通で、拠点(`Base`)からの援護射撃を廃止。「丸腰に見えないための
  最低限の抑止力」として実装していたが、ユーザーの要望により全廃した。`Base`クラスの
  `dmg`/`range`/`rate`/`cd`プロパティと`update()`内の索敵・弾発射ロジックを削除し、
  拠点はもう一切攻撃しない
- [x] セントリー(射程230)がボスの召喚した雑魚に目移りしてターゲットを奪われる不具合を
  修正。攻撃対象のロックオン距離`TARGET_LOCK_RANGE`(210)がセントリーの射程より短かった
  ため、まだ射程内で攻撃を続けられる相手なのにロックだけ先に外れ、近くに湧いた雑魚を
  拾ってしまっていた。ロック判定を`Math.max(TARGET_LOCK_RANGE, this.range)`に変更し、
  自身の射程がTARGET_LOCK_RANGEより長いユニットは射程を優先するようにした
  (`Unit.update()`)。同じ理屈で発生しうるボス側の継続照射(`Boss.update()`の`beam`分岐)
  にも同様の修正を適用した

### 追加実装（v2.8）

- [x] AIのユニット個別レベル投資(元はVERSUS限定)を、1ラウンド目から機能するように
  修正し、SURVIVALにも拡張した。`pickAiLevelTarget()`は「現在の編成で最も多い種類」
  から対象を選ぶ仕組みのため、まだ何も購入していない1ラウンド目や、SURVIVALの
  「毎ラウンド資金全額で組み直す」直後は編成が空で対象を選べず、投資が空振りして
  いた。`buildAiRoster()`の処理順序を「収入からレベル投資予算を先に取り分け→通常の
  ユニット購入→購入後の編成から対象を選んでレベル投資(使い切れなければ通常予算へ
  戻す)」に変更し、1ラウンド目から機能するようにした。SURVIVALは`state.aiUnitLevels`
  も毎ラウンドリセットしてから組み直す(「メカベラム方式」の一貫性を保つため、レベル
  投資も含めて毎回ゼロから組み直す)。EASY(levelInvestRatio=0)は従来通り投資しない
  - 【経緯】初手にナイト3体を横並び配置し、初期資金ぴったりで個別レベルを2回上げる
    という開き方が「1ラウンド目には必ず勝てる」ことを確認したが、これはVERSUS初期
    予算(80G)がちょうどこの配分で使い切れる構造的な結果であり、2ラウンド目以降は
    AIの物量に押し切られるため許容する方針とした。一方で「AI側もこの手のレベル
    投資をしてこないのは違和感がある」との指摘を受け、AI側の投資ロジックが1ラウンド
    目に機能しない実装上の制約を修正した

### 追加実装（v2.9）— 下部バーのレイアウト崩れを精査して修正

「上限なしっていう表示いらないです。改行されてレイアウト崩れます。バトル開始
ボタンも改行してます」との指摘を受け、準備フェーズ下部バー全体を精査した。

- [x] SURVIVAL/VERSUSの配置数表示から「（上限なし）」の文言を削除
  (`配置0体（上限なし）`→`配置0体`)。VERSUS開始時のトースト文言からも同様に
  「配置上限なし」を削除した。上限自体(実質無制限)は変えていない
- [x] 「バトル開始」ボタンが2行に折り返される不具合を修正。原因は下部バー
  (🏠 / 予算・配置数 / ↺ / ショップ / バトル開始 の5要素)の合計幅が画面幅
  ぎりぎりで、ボタンのテキストエリアが1行分に足りていなかったこと。
  `#prep-bar`のgapを8px→6px、`.btn`のpaddingを詰め、`.btn.primary`
  (バトル開始)はfont-size 15px→14px・letter-spacing 0・padding縮小・
  `white-space:nowrap`を追加してテキストの折り返しを防いだ
- [x] 併せて金額表示(`.gold-box`)から「予算」という補助ラベルを削除
  (`300G 予算`→`300G`)。ショップシート側の金額表示(`.sheet-gold`)は
  元々ラベル無しの表記だったため、この変更で表記を統一した。ラベル込みの
  表示は4桁以上のゴールド(繰り越しが進むSURVIVAL/VERSUSで普通に起こる)で
  同じ理由により2行に折り返されるレイアウト崩れがあったため、実質的な
  バグ修正でもある
- [x] タイトル・あそびかた・ユニット図鑑・ショップ(ユニット/強化/戦術タブ)・
  バトル中バー・一時停止・結果画面など主要な画面をひととおりスクリーン
  ショットで確認し、他に同種の折り返し崩れが無いことを確認した

### 追加実装（v2.10）— STORYバランス調整・VERSUS/SURVIVALのAI購入力バグ修正

ユーザーからの相談「ゴブリンキングが強すぎる」「セントリーでボスを瞬殺できる」
「STORY EXTRAの差が分からない」「敵の使用ユニットが少ない」を1件ずつ検証し、
全て事実として確認できたため、方向性の合意を得たうえで対応した。

- [x] ゴブリンキング(ボス4)の雑魚召喚間隔を300→450フレーム(5秒→7.5秒)に緩和。
  本編最初のボスであるにも関わらず、準備期間のボス3(1体/6.67秒)より濃い
  召喚レートになっていたことが「一番きつい」体感の主因と判断し、本体の
  HP・攻撃力・薙ぎ払いは弱体化しすぎないよう据え置いた
- [x] セントリーの継続照射(`beamRampCap:Infinity`)が、動きが遅くロックの
  外れにくいボス相手だと際限なく伸びてしまい、単体で全ボスを瞬殺できる
  水準(60秒ロックで約4.4万ダメージ)だったことを検証で確認。`beamRampCapVsBoss`
  (5倍)を新設し、対象が`state.boss`のときだけ有限のランプ上限を適用するよう
  `Unit.update()`を修正した(雑魚戦の無制限ランプは変更なし)。修正後は60秒で
  約7500ダメージとなり、ゴブリンキング程度は倒せるがラスボスは単体で
  瞬殺できない水準に収まる
- [x] STORY EXTRAの雑魚数倍率(`STORY_EXTRA_ENEMY_MULT`)を1.15→1.2に、
  丸め方を`Math.round`→`Math.ceil`に変更。STORY_STAGESの大半のグループは
  1〜3体の少人数で、四捨五入だと端数が消えて増加0のグループが大半になり
  「通常と差が分からない」原因になっていた。切り上げに変えたことで
  count>=1の全グループに最低+1体が乗るようになった
- [x] STORY全10ステージを通して敵が goblin/orc/skeleton/archer/wizard/
  healer/giant の7種類しか使っておらず、エリート7種(セントリー・ストーン
  ガーディアン・ウォーロード・リッチ・ドレイク・インプ・ロード)とナイトが
  一度も登場していなかったことを確認。ステージ7にインプ、8にストーン
  ガーディアン、9にリッチ、10にナイト・ウォーロード・セントリーを各1〜2体
  ずつ追加し、後半に向けて敵の顔ぶれに幅を持たせた(STORY_STAGES_EXTRAは
  STORY_STAGESから自動生成されるため、EXTRA側にも自動的に反映される)
- [x] 【重要】VERSUS/SURVIVALでAIが弱体化していた回帰バグを修正。v2.8で
  「AIの個別レベル投資を1ラウンド目から機能させる」対応をした際、レベル
  投資予算を購入ループの**前**に無条件で天引きする方式に変えてしまい、
  結果として毎ラウンド(1ラウンド目に限らず)収入の一部(HARDなら最大45%)が
  購入予算から差し引かれるようになっていた。しかも天引き分がレベル上限等で
  使い切れなかった場合、購入ループが終わったあとに返金されるため、その回の
  購入には一切使われず死蔵されていた。これが「敵が弱くなった」「セントリーや
  ゴーレムを使ってこない」「編成に対策してこない」という体感の直接の原因。
  旧来の「その場で使った分だけaiGoldから引く」ライブ消費方式に戻しつつ、
  まだ編成が無い場合(VERSUSの1ラウンド目・SURVIVALの毎ラウンド組み直し直後)
  だけは新設の`pickAiLevelTargetFromPool()`で購入プールの中の最大ウェイトの
  ユニットを暫定の投資対象とすることで、v2.8の本来の目的(1ラウンド目からの
  投資)も両立させた

### 今後の課題（任意）

- [ ] BGM / 効果音
- [ ] リプレイやスコアランキング
- [ ] 部隊（分隊）制 / グリッド配置（ユーザーと相談の上、一旦保留）

---

## Cloudflare Pages 設定メモ

- ビルドコマンド: なし（静的ファイルをそのまま配信）
- ビルド出力ディレクトリ: `/`（ルートディレクトリ）
- 環境変数: 不要
- main ブランチへの push で自動デプロイが走る

---

## PWA 対応

- `manifest.json` を `index.html` から読み込む
- アイコン: Knight（歩兵）スプライトをドット絵で描画して PNG 生成（`icons/` に配置）
- テーマカラー: `#0f172a`（ダークネイビー）
